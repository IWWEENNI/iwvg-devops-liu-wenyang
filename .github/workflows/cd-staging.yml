name: CD Staging
on:
  push:
    branches: ['staging']

env:
  REPOSITORY: ${{ github.repository }}
  GHCR_URL: ghcr.io

jobs:
  cd-staging:
    name: Build & Push Docker image (staging)
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Extract artifact from pom
        run: |
          echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_ENV
          echo "ARTIFACT=$(mvn help:evaluate -Dexpression=project.artifactId -q -DforceStdout)" >> $GITHUB_ENV
          echo "REPOSITORY_LC=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_URL }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PERSONAL_ACCESS_TOKEN }}

      - name: Build & Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.GHCR_URL }}/${{ env.REPOSITORY_LC }}:staging-${{ env.VERSION }}
            ${{ env.GHCR_URL }}/${{ env.REPOSITORY_LC }}:staging

      # 🌐 Render Deployment
      # 1. En Render: Web Service → Settings → Deploy Hook → copiar el token
      # 2. En GitHub: Settings → Secrets and variables → Actions → crear secret: RENDER_DEPLOY_HOOK_TOKEN
      #- name: 🚀 Deploy on Render
      #  run: |
      #    curl --fail -X POST "https://api.render.com/deploy/${{ secrets.RENDER_DEPLOY_HOOK_TOKEN }}" \
      #    && echo "✅ Deployment succeeded" \
      #   || (echo "❌ Deployment failed" && exit 1)
